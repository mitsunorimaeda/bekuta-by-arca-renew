/*
  # クーパーテストの単位修正：VO2max表示に変更

  ## 概要
  クーパーテストの単位を走行距離(m)からVO2max(ml/kg/分)に変更します。
  走行距離は入力フィールドとして保持し、primary_valueにはVO2maxの計算値を格納します。

  ## 変更内容
  1. クーパーテストの単位を「m」から「ml/kg/分」に変更
  2. VO2max計算式: VO2max = (距離 - 504.9) / 44.73
     - この式は12分間走行距離からVO2maxを推定する標準的な式です
  3. フィールド定義はそのまま（距離入力）

  ## セキュリティ
  - 既存のRLSポリシーを維持
  - データ構造の変更のみ
*/

-- クーパーテストの単位を修正
UPDATE performance_test_types
SET 
  unit = 'ml/kg/分',
  description = '12分間でできるだけ遠くまで走り、距離を測定します。VO2max（最大酸素摂取量）を計算します。計算式: VO2max = (距離 - 504.9) / 44.73'
WHERE name = 'cooper_test';
